{
  "name": "rebypass-build",
  "version": "1.6.1",
  "description": "Build script for ReBypass userscript",
  "main": "build.js",
  "scripts": {
    "build": "node scripts/sync-matches.js && node build.js"
  },
  "dependencies": {
    "badge-maker": "^3.3.1"
  }
}