{
  "name": "rebypass-build",
  "version": "1.8.7",
  "description": "Build script for ReBypass userscript",
  "main": "build.js",
  "scripts": {
    "build": "bun scripts/sync-matches.js --sync && bun build.js"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "badge-maker": "^5.0.2",
    "puppeteer": "^24.34.0"
  }
}
