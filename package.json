{
  "name": "rebypass-build",
  "version": "1.7.9",
  "description": "Build script for ReBypass userscript",
  "main": "build.js",
  "scripts": {
    "build": "node scripts/sync-matches.js --sync && node build.js"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "badge-maker": "^5.0.2",
    "puppeteer": "^24.34.0"
  }
}
