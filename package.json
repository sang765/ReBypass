{
  "name": "rebypass-build",
  "version": "1.9.7",
  "description": "Build script for ReBypass userscript",
  "main": "build.ts",
  "scripts": {
    "build": "bun scripts/sync-matches.ts --sync && bun build.ts",
    "build:no-update": "bun scripts/sync-matches.ts --sync && bun build.ts no-update",
    "sync-userscripts": "bun scripts/sync-userscripts.ts"
  },
  "dependencies": {
    "@genkit-ai/mcp": "^1.28.0",
    "@playwright/test": "^1.57.0",
    "badge-maker": "^5.0.2",
    "puppeteer": "^24.34.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
